{% extends 'base.html.twig' %}

{% block title %}Rapportmedicament{% endblock %}

{% block body %}
    <h1>Rapportmedicament</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rapportmedicament.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ rapportmedicament.quantite }}</td>
            </tr>
            <tr>
                <th>Estechantillon</th>
                <td>{{ rapportmedicament.estechantillon ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Estdocumente</th>
                <td>{{ rapportmedicament.estdocumente ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rapportmedicament_index') }}">back to list</a>

    <a href="{{ path('app_rapportmedicament_edit', {'id': rapportmedicament.id}) }}">edit</a>

    {{ include('rapportmedicament/_delete_form.html.twig') }}
{% endblock %}
